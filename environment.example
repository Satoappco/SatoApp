# Environment Configuration Example
# Set these environment variables in your deployment environment

# OpenAI API Key (required for CrewAI)
OPENAI_API_KEY=your_openai_api_key_here

# Webhook Authentication
API_KEY=SatoLogos

# Database Configuration - Option 1: Full connection string
# DATABASE_URL=postgresql://username:password@host:port/database_name

# Database Configuration - Option 2: Individual components
DB_HOST=your-cloud-sql-instance-ip
DB_PORT=5432
DB_NAME=sato_db
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Google Cloud SQL specific (set to "true" if using Google Cloud SQL)
GOOGLE_CLOUD_SQL=true

# Database debugging (set to "true" to enable SQL query logging)
DB_ECHO=false

# Application Port (automatically set by Google Cloud Run)
PORT=8080
