# Requirements for Google Ads MCP HTTP Server
#
# This file contains dependencies needed to run the HTTP/SSE server
# in addition to the base MCP server dependencies.

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# For SSE (Server-Sent Events)
sse-starlette>=1.8.0

# HTTP client for testing and outbound requests
httpx>=0.25.0

# Pydantic for request/response models (usually comes with FastAPI)
pydantic>=2.0.0

# Google Ads API
google-ads>=25.0.0

# Base MCP dependencies (from google_ads_mcp)
fastmcp>=0.1.0
pyyaml>=6.0
